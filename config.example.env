# RPC endpoint URL
# Example for local node: http://localhost:8545
# Example for Infura: https://mainnet.infura.io/v3/YOUR_PROJECT_ID
# Example for Alchemy: https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY
RPC_URL=https://your-rpc-endpoint.com

# DID Contract address
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Batch size for multicall (how many calls to bundle together)
# Higher values = fewer RPC calls but larger request size
# Recommended: 250-500
BATCH_SIZE=500

# Number of concurrent batch requests
# Higher values = faster but more network load
# Recommended: 3-10
CONCURRENCY=5

# Number of retry attempts for failed requests
RETRY_ATTEMPTS=3

# Output file path (optional, defaults to domains.json)
OUTPUT_FILE=domains.json

